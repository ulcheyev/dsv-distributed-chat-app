syntax = "proto3"; /* default is proto2 */
option java_package = "proto";
service test3 {
  rpc SayHello (test) returns (test2) {}
}
message test {
  /*type   name = id; */
  string name = 1; /* Id must be bigger than 0 and be the same on client and server */
}
message test2 {
  string message = 1;
}